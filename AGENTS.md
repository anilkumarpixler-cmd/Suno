# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

Project Cursor rules live in `.cursor/rules/`:
- `code-standards.mdc` — TypeScript, Expo 57, structure
- `code-quality.mdc` — types, errors, cleanup, lint
- `reuse.mdc` — reuse existing components and methods
- `e2e-testing.mdc` — end-to-end flow coverage

